{% if post is not null %}
    <a href="{{ path('take',{"postId":post.id}) }}" style="display:block" class="media">
    <div class="media-body">
            <h3>Défi #{{ nb }}</h3>
    Prendre un selfie avec
    {% for k,user in post.users %}
        {{ user.firstName ~ ' ' ~ user.lastName }}{% if k < (post.users|length)-1 %}, {% endif %}
    {% endfor %}
    </div>
    <div class="media-right">

            <i class="fa fa-chevron-right" aria-hidden="true"></i>

    </div>
    </a>
    <hr>
{% endif %}
